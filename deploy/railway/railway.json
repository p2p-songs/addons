{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "Health check + restart policy for the musicmeta service. Build note in README.md: Railway's native Dockerfile builder cannot see the sibling addon-sdk, so deploy the prebuilt image (recommended today) — this file governs the deploy half either way.",
  "deploy": {
    "healthcheckPath": "/manifest.json",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
